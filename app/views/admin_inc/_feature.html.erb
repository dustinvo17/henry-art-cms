
<div class="container mx-auto">
  <p class="text-center py-12  mx-auto text-6xl uppercase font-thin tracking-widest font-mono">
    <%= @setting.s1 %>
  </p>
  <div class="portfolio-products py-12 px-12">
   <% @imgids.each do |img|%>
   
      <div>
      <a href="/admin/feature/<%= img.id %>">
           <%= image_tag @features[img.id]  %>
      </a>
       
      </div>
    <%end%>
  </div>
</div>
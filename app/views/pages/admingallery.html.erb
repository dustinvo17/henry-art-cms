<%=render 'admin_inc/navbar'%>

<div class="container mx-auto py-24 ">
    <div class="flex flex-row justify-between">
        <div class="text-2xl ">
        <%= @user.images.length %> Photos
        </div> 
        <div class="flex flex-col">
         <%= form_with(url: '/images/upload',method:'post', html:{class:"form-gallery"} ) do |f|%>
            <%= f.file_field :images, multiple: true, accept: "image/jpeg, image/jpg, image/gif, image/png" %>
            <%= f.submit 'Add photos', class:"p-2 bg-green-400 text-white rounded cursor-pointer"%>
        <% end %>
          
        </div>

    </div>
    <hr class="my-10">
    <%= render 'admin_inc/gallery'%>
</div>

<script>



</script>